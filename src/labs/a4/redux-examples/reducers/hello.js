/* 
reducer that provides static data. A reducer is a function that takes the current state and an action, and then returns a new state.
*/

const hello = () => ({message: 'Hello World'});
export default hello;